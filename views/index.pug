extends layout
block content
  header
    #title
      h1
      a(href='/') flo app
    nav
      h2
      a(href='/log-in') Log-in
      h2
      a(href='/sign-up') Sign-Up
      if user 
        p=user 
